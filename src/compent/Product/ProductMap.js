import React from 'react'

const ProductMap = ({el}) => {
    return (
        <div>
            <div className="FlexBox">
                <img src={el.image}/>
            
            </div>
        </div>
    )
}

export default ProductMap
